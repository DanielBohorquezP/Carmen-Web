import { ContactPage } from "@/components/ContactPage";

export const metadata = {
  title: "Contact — Well Brain",
  description: "Escríbele a Carmen Susana.",
};

export default function Contact() {
  return (
    <main>
      <ContactPage />
    </main>
  );
}
